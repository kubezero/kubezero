---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../modules/aws/eks/cluster
  - ../../modules/aws/eks/provider

patches:
  - path: patch-cluster-secret-name.yaml
    target:
      kind: ExternalSecret
      name: cluster-secret
